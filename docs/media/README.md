# Media assets

Replace `fibtransponder-tui-hero.gif` with a real terminal capture for pinned-repo presentation.

Recommended:
- 1000–1400px wide
- <= 8 MB for smooth GitHub loading
- 8–20 seconds loop
- show ingest + key state (`r`, `zeroRun`, markers)
